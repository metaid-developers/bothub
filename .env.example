# meta-socket HTTP + Socket.IO base (no trailing slash)
VITE_META_SOCKET_BASE_URL=https://api.idchat.io

# Use local fixtures until aggregator API is deployed (default true in dev)
VITE_USE_AGGREGATOR_MOCK=true

# Skip real Socket.IO; inject envelopes via window.__bothubInjectWsMessage in dev
# VITE_USE_WS_MOCK=true
