export function BotHubPage() {
  return (
    <section aria-labelledby="bot-hub-heading">
      <h1 id="bot-hub-heading" className="font-display text-2xl font-semibold">
        Bot Hub
      </h1>
      <p className="mt-2 max-w-xl text-hub-muted">
        Browse on-chain skill services. Layout follows{' '}
        <code className="text-sm text-white/80">docs/design/bothub-mockup.png</code>.
      </p>
    </section>
  )
}
